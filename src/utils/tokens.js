// Token Utilities
// - generateAccessToken(payload): sign a short-lived JWT (e.g. 15m)
// - generateRefreshToken(payload): sign a long-lived JWT (e.g. 7d)
// - verifyToken(token, secret): verify and decode a JWT
